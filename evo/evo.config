[

 {sasl, [
    {sasl_error_logger, {file, "sasl_err.log"}}
 ]},

 {evo, [
    {sites, [
        {routecomplete, [
            {port, 8081},
            {dbinfo, {"routecomplete_dev", "routecomplete", "secret"}},
            {components, [
                {"static", module, {evostatic, ["static"]}},

                {"customers", gen_server, {evotable, evotable_customers,
                                           [{table, customers},
                                            {listCols, [id, {name, view}, logo]}
                                           ]}},

                {"events", gen_server, {evotable, evotable_events,
                                        [{table, events},
                                         {listCols, [{id, view}, lat, lon]}
                                        ]}}
            ]},

            {templates, [
                {site, {"text/html", reload, "templates/auto/site.html"}}
            ]}
        ]}
    ]}
 ]}

].