[

 {sasl, [
    {sasl_error_logger, {file, "err.log"}}
 ]},

 {evo, [
    {sites, [
        {routecomplete, [
            {port, 8081},
            {dbinfo, {"routecomplete_dev", "routecomplete", "secret"}},
            {components, [
                {"static", module, {evostatic, ["static"]}},
                {"customers", gen_server, {evotable, evotable_customers,
                                           [customers, [id, name, logo], {auto, auto}]}}
            ]},
            {templates, [
                {admin, {"text/html", {reload, "templates/admin.html"}}}
            ]}
        ]},

        {rimowa, [
            {port, 8082},
            {dbinfo, {"rimowa", "brendonh", "arthur"}}
        ]}
    ]}
 ]}

].